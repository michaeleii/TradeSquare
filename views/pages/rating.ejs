<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/styles/styles.css" rel="stylesheet" />
    <link rel="icon" type="image/x-icon" href="/images/icons/favicon.svg" />
    <title><%= user.f_name %>'s Ratings</title>
  </head>
  <body class="bg-bkg text-content">
    <article class="w-[90%] mx-auto md:w-[60%]">
      <%- include('../components/header.ejs', {isAuthenticated}) %>
      <div class="flex flex-col md:flex-row justify-between mb-3 items-center">
        <h1><%= user.f_name %>'s Ratings</h1>
        <a
          href="rating/create"
          class="btn-primary rounded-lg w-full md:w-fit text-center cursor-pointer"
        >
          Write a Review
        </a>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
        <% for( let i = 0; i < 9; i++ ) { %> <%-
        include('../components/ratingCard.ejs') %> <% } %>
      </div>
    </article>
    <%- include("../components/navigationBar", {user}) %>
  </body>
</html>
